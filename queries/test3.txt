NO DIFFERENCE IN QEP DESPITE CHANGE IN WHERE CLAUSE
q1 = '''
EXPLAIN (VERBOSE, ANALYZE, FORMAT JSON)
select
      o_orderpriority,
      count(*) as order_count
    from
      orders
    where
      o_totalprice > 100
    group by
      o_orderpriority
    order by
      o_orderpriority;
'''

q2 = '''
EXPLAIN (VERBOSE, ANALYZE, FORMAT JSON)
select
      o_orderpriority,
      count(*) as order_count
    from
      orders
    where
      o_totalprice > 100
      and extract(year from o_orderdate) =  1996
    group by
      o_orderpriority
    order by
      o_orderpriority;
'''